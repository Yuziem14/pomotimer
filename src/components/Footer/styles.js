import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  footer: {
    alignItems: 'center',
    marginTop: 30,
  },
  text: {
    color: '#333',
    fontSize: 24,
    fontWeight: 'bold',
  },
  roundCounter: {
    color: '#fff',
  },
})

export default styles
